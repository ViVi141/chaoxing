# ============================================
# 部署模式配置
# ============================================
# simple: SQLite + 文件队列（推荐，无需额外依赖）
# standard: PostgreSQL + Redis（生产环境）
DEPLOY_MODE=simple

# ============================================
# 简单模式配置（默认）
# ============================================
DATABASE_URL=sqlite+aiosqlite:///./chaoxing_web.db
CELERY_BROKER_URL=filesystem://
CELERY_RESULT_BACKEND=file://./celery_results

# ============================================
# 标准模式配置（需要时取消下面注释并注释掉上面的配置）
# ============================================
# 数据库配置
# POSTGRES_PASSWORD=your_secure_password_here
# DATABASE_URL=postgresql+asyncpg://chaoxing_user:your_secure_password_here@postgres:5432/chaoxing_db

# Redis配置
# REDIS_PASSWORD=your_redis_password_here
# REDIS_URL=redis://:your_redis_password_here@redis:6379/0

# Celery配置
# CELERY_BROKER_URL=redis://:your_redis_password_here@redis:6379/0
# CELERY_RESULT_BACKEND=redis://:your_redis_password_here@redis:6379/0

# 安全密钥（生产环境必须修改！）
SECRET_KEY=your-very-long-and-random-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here

# 应用配置
DEBUG=False
LOG_LEVEL=INFO

# CORS配置（多个用逗号分隔）
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-domain.com

# 前端配置
VITE_API_URL=http://localhost:8000

# 管理员默认账号（首次登录后立即修改密码！）
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=Admin@123

# 任务配置
MAX_CONCURRENT_TASKS_PER_USER=3
TASK_TIMEOUT=7200

# 生成安全密钥的Python命令:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

