# 环境变量配置文件模板
# 使用方法：复制此文件为 .env 并修改为你的实际配置
# cp env.template .env

# ===== 外部数据库配置 =====

# PostgreSQL数据库连接（必需修改）
# 格式：postgresql+asyncpg://用户名:密码@容器名或IP:端口/数据库名
DATABASE_URL=postgresql+asyncpg://chaoxing_user:your_password@1Panel-postgresql-LEsZ:5432/chaoxing_db

# Redis连接（必需修改）
# 格式：redis://:密码@容器名或IP:端口/数据库编号
REDIS_URL=redis://:your_redis_password@1Panel-redis-71AP:6379/0
CELERY_BROKER_URL=redis://:your_redis_password@1Panel-redis-71AP:6379/0
CELERY_RESULT_BACKEND=redis://:your_redis_password@1Panel-redis-71AP:6379/0

# ===== 应用密钥（必需修改！安全重要！） =====

# 生成新密钥：
# Windows PowerShell:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
# Linux/Mac:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"

SECRET_KEY=请替换为随机生成的密钥
JWT_SECRET_KEY=请替换为随机生成的密钥

# ===== 可选配置 =====

# 调试模式（生产环境必须设置为False）
DEBUG=False

# 日志级别
LOG_LEVEL=INFO

# ===== 如何查找数据库容器名/IP =====

# 1. 查看PostgreSQL容器信息：
#    docker inspect 1Panel-postgresql-LEsZ
#
# 2. 查看Redis容器信息：
#    docker inspect 1Panel-redis-71AP
#
# 3. 查看容器IP地址：
#    docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' 容器名
#
# 4. 如果容器名无法解析，使用IP地址：
#    DATABASE_URL=postgresql+asyncpg://user:pass@172.17.0.5:5432/db
#    REDIS_URL=redis://:pass@172.17.0.6:6379/0

