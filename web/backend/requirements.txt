# ================================
# Web后端依赖文件
# 
# 注意：请在项目根目录安装依赖！
# cd ../../  # 返回项目根目录
# pip install -r requirements.txt
# 
# 本文件仅用于Docker构建时的独立安装
# ================================

# 引用根目录的统一依赖
# 如果需要在此目录独立安装，请使用：
# pip install -r ../../requirements.txt
